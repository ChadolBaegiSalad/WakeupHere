/** @format */

import {AppRegistry} from 'react-native';
import WakeupHere from './WakeupHere';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => WakeupHere);
